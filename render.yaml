services:
  - type: web
    name: codesmith-backend
    env: java
    buildCommand: mvn clean install
    startCommand: java -jar target/codesmith-1.0.0.jar
    envVars:
      - key: JAVA_TOOL_OPTIONS
        value: -Xmx512m
      - key: PORT
        value: 8080
